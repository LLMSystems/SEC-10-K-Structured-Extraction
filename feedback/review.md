1. 需要針對驗證的部分，提出一些最基本、最可靠的驗證方法，例如從人驗證角度來看，人會去看字數是不是差不多，一個item的頭尾有沒有被覆蓋到，對應的頁碼有沒有跳來跳去，需要從第一性原理找一些可以快速驗證解析是否正確的(接近100%)，目前使用的rule(docs/validator-rules.md)，是從歷史資料統計結果來定規則，沒辦法100。應該要有一些rule夠簡單可以快速確定解析是否正確
2. 是否可以通過多模態模型(在預算無限的情況下)來解析(例如請多模態模型先解析table of content)，請多模態去抓出每個item的第一句跟最後一句，是否能做出一個很強的驗證器，自動驗證。